<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gong.concert.concert.mapper.SeatMapper">

    <insert id="insertSeat">
        INSERT INTO seats(seat_id, concert_id, fee, seat_status, seat_type, seat_row, seat_col)
        VALUES (#{seatId},
                #{concertId},
                #{fee},
                #{seatStatus},
                #{seatType},
                #{seatRow},
                #{seatCol})
    </insert>
</mapper>