<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gong.concert.order.mapper.OrderDetailMapper">

    <insert id="insert">
        insert into order_detail(order_detail_id, name, image, order_id, seat_id, amount, `row`, `col`)
        VALUES(#{orderDetailId},#{name},#{image},#{orderId},#{seatId},#{amount},#{row},#{col})
    </insert>
</mapper>